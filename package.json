{
  "name": "credito-sindicato",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "deploy": "npm run build && npx wrangler deploy"
  }
}
